# Obsidian base64 conversion Plugin

This plugin converts base64 encoded text to utf-8 encoded text which is to simple text.

# Installation

copy paste the `main.js`, `manifest.json` and `styles.css`
